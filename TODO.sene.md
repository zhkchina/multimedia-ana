scene 服务其实一个命令就可以搞定，不需要 docker
但是考虑到未来使用模型检测的方式，还是有必要放一个 docker 用来隔离环境的
考虑到这个服务与其它服务并行，任务内串行，因此 api 独立出来
未来有了重模型占用显存资源时，再与现有 api 结合拉起 worker。

推荐目前的使用方式：docker + cli 模式
目前这个模块的接口定义、测试只是跑通了流程，搭起框架，尤其是接口定义还没有明确。
